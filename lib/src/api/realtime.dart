/// Realtime channels
class RealtimeChannels {
  /// Channel to listen to for save user events
  static const saveAuth = 'auth:save';

  /// Channel to listen to for delete user events
  static const deleteAuth = 'auth:delete';

  /// Channel to listen to for save session events
  static const saveSession = 'session:save';
}
